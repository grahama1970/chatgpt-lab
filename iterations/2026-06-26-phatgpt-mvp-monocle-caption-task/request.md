# PhatGPT MVP Monocle Caption Task

This branch is an implementation-ready task carrier for the PhatGPT MVP loop.

The coder worker must apply the task described in the pull request body, not
infer broader design or rebuild work from this file. The intended mutation is a
single exact text replacement in `monocle-man-site/src/main.jsx`, followed by
the declared validation commands and a PR comment receipt.

The reviewer worker must remain read-only and comment a verdict on the same PR.
